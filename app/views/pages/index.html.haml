- table(@pages, ['url', 'title', '']) do |page, klass|
  %tr{:class => klass}
    %td= page.url
    %td= page.title
    %td
      = link_to 'Edit', edit_page_path(page)
      = link_to 'Delete', page_path(page), :method => :delete, :confirm => "Are you sure?"
.link
  = link_to 'New Page', new_page_url
