.page
	- if @page
		= render :partial => 'content_fragments/page', :object => @page
	- else
		Create a page with a url of "/about_us/national_board" to not see this page anymore.
.page.national_board_member_content
	= render :partial => 'users/board_member', :collection => User.national_board_members
