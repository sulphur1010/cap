.page
	%h2 All Encyclicals
	- table(@encyclicals, ['name', 'title']) do |encyclical, klass|
		%tr{:class => klass}
			%td= link_to encyclical.name, encyclical_path(encyclical)
			%td= encyclical.title
